/**
 * Supabase Auto-Generated Database Types Stub
 * Inner G Complete Agency — Client Intelligence Portal
 *
 * This file will be REPLACED by the auto-generated output of:
 *   npx supabase gen types typescript --project-id <ref> > types/database.ts
 *
 * Once the Supabase project is created and migrations are applied, run that
 * command to get fully typed database queries via the Supabase JS SDK.
 *
 * For now, this stub re-exports from our hand-authored types so the
 * codebase doesn't break before the Supabase project exists.
 */

export type { User, Client, Project, Campaign, CampaignMetric, AiSignal, GrowthAuditLead, ActivityLogEntry, ChatSession, ChatMessage } from "./index"

/**
 * Placeholder for Supabase generated DB type — will be replaced.
 * Usage: createClient<Database>(url, key)
 */
export type Database = Record<string, unknown>
